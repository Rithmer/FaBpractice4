# ТехМаркет — Интернет-магазин техники

Full-stack приложение интернет-магазина с микросервисной архитектурой, балансировкой нагрузки, кэшированием и использованием нескольких СУБД.

**Выполнил:** Константинов Илья, группа ЭФБО-10-24
**Дисциплина:** Фронтенд и бэкенд разработка
**Семестр:** 4, 2025/2026 уч. год

---

## Что реализовано по практикам

### Практика 19 — PostgreSQL (реляционная БД)

**Для чего:** хранение пользователей и токенов с чёткой структурой и связями.

* Используется PostgreSQL.
* Таблицы:

  * `users`
  * `refresh_tokens`
* Реализована работа через SQL.
* Все маршруты пользователей связаны с БД.

---

### Практика 20 — MongoDB (NoSQL)

**Для чего:** гибкое хранение товаров без жёсткой схемы.

* Используется MongoDB.
* Коллекция:

  * `products`
* CRUD операции реализованы через ODM.
* Данные товаров полностью перенесены из SQL.

---

### Практика 21 — Redis (кэширование)

**Для чего:** ускорение ответов и снижение нагрузки на БД.

* Подключён Redis.
* Реализовано кэширование:

  * `GET /api/products`
* TTL кэша: ~60 секунд.
* Добавлена инвалидация кэша при изменении данных.
* Заголовок ответа:

  * `X-Cache: HIT / MISS`

---

### Практика 22 — Балансировка нагрузки (Nginx)

**Для чего:** распределение запросов и отказоустойчивость.

* Используется Nginx как reverse proxy.
* Запущено **2 backend-сервера**:

  * `backend1`
  * `backend2`
* Алгоритм: Round Robin.
* Проверка через `/health`.

---

### Практика 23 — Docker Compose (контейнеризация)

**Для чего:** удобный запуск всей системы.

* Все сервисы упакованы в Docker:

  * backend1
  * backend2
  * frontend
  * nginx
  * mongo
  * redis
* Запуск одной командой:

  ```bash
  docker compose up --build
  ```

---

## Архитектура

* **Frontend:** React
* **Backend:** Node.js + Express (2 экземпляра)
* **Балансировщик:** Nginx
* **БД:**

  * PostgreSQL — пользователи
  * MongoDB — товары
* **Кэш:** Redis

---

## Роли

* `guest` — неавторизован
* `user` — просмотр товаров
* `seller` — управление товарами
* `admin` — управление системой

---

## Основные API

### Аутентификация

* `POST /api/auth/login`
* `POST /api/auth/refresh`
* `GET /api/auth/me`

### Пользователи (PostgreSQL)

* `GET /api/users`
* `GET /api/users/:id`
* `PUT /api/users/:id`
* `DELETE /api/users/:id`

### Товары (MongoDB)

* `GET /api/products`
* `POST /api/products`
* `PUT /api/products/:id`
* `DELETE /api/products/:id`

---

Да, это обязательно надо добавить — иначе у него реально не запустится. Вставь как отдельный блок перед запуском.

Вот как лучше оформить в README (чётко и по делу 👇):

---

## Важно: генерация SSL-сертификатов

Проект использует HTTPS (Nginx).
Перед первым запуском необходимо создать локальные сертификаты.

### Создание сертификатов

```bash
mkdir -p nginx/certs

openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout nginx/certs/key.pem \
  -out nginx/certs/cert.pem \
  -subj "/CN=localhost"
```
